# HOL 5: Cleanup, pull and push

Run each command in Git Bash. Replace `<repository-url>` with your own GitHub repository URL.

```bash
git status
git branch -a
git pull origin main
git push origin main
git log --oneline --graph --decorate -10
```
