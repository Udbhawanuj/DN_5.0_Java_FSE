import {useEffect,useState} from 'react';import GitClient from './GitClient';export default function App(){const [repos,setRepos]=useState([]);const [error,setError]=useState('');useEffect(()=>{new GitClient().getRepositories('techiesyed').then(setRepos).catch(e=>setError(e.message))},[]);return <main><h1>GitHub Repositories</h1>{error?<p className="error">{error}</p>:<ul>{repos.map(r=><li key={r.id||r.name}>{r.name}</li>)}</ul>}</main>}