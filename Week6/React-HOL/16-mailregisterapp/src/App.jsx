import {useState} from 'react';export default function App(){const [form,setForm]=useState({name:'',email:'',password:''});const [errors,setErrors]=useState({});const change=e=>setForm({...form,[e.target.name]:e.target.value});const submit=e=>{e.preventDefault();const x={};if(form.name.length<5)x.name='Name should have at least 5 characters';if(!form.email.includes('@')||!form.email.includes('.'))x.email='Email must contain @ and .';if(form.password.length<8)x.password='Password should have at least 8 characters';setErrors(x);if(!Object.keys(x).length)alert('Registration successful')};return <main><h1>Mail Registration</h1><form className="card" onSubmit={submit}>{['name','email','password'].map(k=><label key={k}>{k[0].toUpperCase()+k.slice(1)}<input type={k==='password'?'password':'text'} name={k} value={form[k]} onChange={change}/>{errors[k]&&<small className="error">{errors[k]}</small>}</label>)}<button>Register</button></form></main>}