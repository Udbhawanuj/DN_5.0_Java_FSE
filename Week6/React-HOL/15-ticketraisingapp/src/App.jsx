import {useState} from 'react';export default function App(){const [name,setName]=useState('');const [complaint,setComplaint]=useState('');const submit=e=>{e.preventDefault();const ref='REF'+Date.now().toString().slice(-6);alert(`Thanks ${name}. Complaint submitted. Reference: ${ref}`);setName('');setComplaint('')};return <main><h1>Register your complaint</h1><form className="card" onSubmit={submit}><label>Employee Name<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Complaint<textarea value={complaint} onChange={e=>setComplaint(e.target.value)} required/></label><button>Submit</button></form></main>}