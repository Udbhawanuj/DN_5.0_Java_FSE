import { Component, EventEmitter, Input, Output } from '@angular/core'; import { CommonModule } from '@angular/common'; import { Course } from '../../models/course'; import { CreditLabelPipe } from '../../pipes/credit-label.pipe'; import { HighlightDirective } from '../../directives/highlight.directive'; @Component({selector:'app-course-card',standalone:true,imports:[CommonModule,CreditLabelPipe,HighlightDirective],template:`<article class="card" appHighlight [ngClass]="{'card--enrolled':course.enrolled,'grade-good':(course.grade||0)>=80,'grade-average':(course.grade||0)>=60&&(course.grade||0)<80,'grade-low':(course.grade||0)<60}"><h3>{{course.name}}</h3><p>{{course.instructor}}</p><p>{{course.credits|creditLabel}} · {{course.category}}</p><button (click)="enroll.emit(course.id)">{{course.enrolled?'Unenroll':'Enroll'}}</button></article>`}) export class CourseCardComponent {@Input({required:true}) course!:Course; @Output() enroll=new EventEmitter<number>();}