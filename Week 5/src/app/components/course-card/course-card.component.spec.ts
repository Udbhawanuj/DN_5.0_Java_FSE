import { ComponentFixture,TestBed } from '@angular/core/testing'; import { CourseCardComponent } from './course-card.component'; describe('CourseCardComponent',()=>{let c:CourseCardComponent;let f:ComponentFixture<CourseCardComponent>;beforeEach(async()=>{await TestBed.configureTestingModule({imports:[CourseCardComponent]}).compileComponents();f=TestBed.createComponent(CourseCardComponent);c=f.componentInstance;c.course={id:1,name:'Angular',instructor:'Asha',credits:3,category:'Frontend',enrolled:false};f.detectChanges()});it('creates',()=>expect(c).toBeTruthy());it('shows name',()=>expect(f.nativeElement.textContent).toContain('Angular'));it('shows credits',()=>expect(f.nativeElement.textContent).toContain('3 Credits'));it('emits id',()=>{spyOn(c.enroll,'emit');f.nativeElement.querySelector('button').click();expect(c.enroll.emit).toHaveBeenCalledWith(1)});it('shows Enroll',()=>expect(f.nativeElement.querySelector('button').textContent).toContain('Enroll'))});