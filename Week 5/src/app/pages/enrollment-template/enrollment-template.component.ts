import { Component } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; @Component({standalone:true,imports:[CommonModule,FormsModule],template:`<section class="container"><h2>Template-driven Enrollment</h2><form #f="ngForm" (ngSubmit)="submit(f.value)" class="card"><label>Name</label><input name="studentName" required minlength="3" ngModel #name="ngModel"><div class="error" *ngIf="name.touched&&name.invalid">Name must contain at least 3 characters.</div><label>Email</label><input name="email" required email ngModel #email="ngModel"><div class="error" *ngIf="email.touched&&email.invalid">Enter a valid email.</div><label>Course</label><select name="course" required ngModel><option value="">Choose</option><option>Angular Fundamentals</option><option>Spring Boot</option></select><label><input style="width:auto" type="checkbox" name="terms" required ngModel> Accept terms</label><button [disabled]="f.invalid">Submit</button></form></section>`}) export class EnrollmentTemplateComponent {submit(v:unknown){console.log(v)}}