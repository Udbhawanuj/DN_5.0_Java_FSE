import { Component } from '@angular/core'; import { AsyncPipe, NgFor } from '@angular/common'; import { EnrollmentService } from '../../services/enrollment.service'; @Component({standalone:true,imports:[AsyncPipe,NgFor],template:`<section class="container"><h2>Student Profile</h2><div class="card"><p>Name: Udbhaw Anuj</p><p>Program: Java FSE</p></div><h3>Enrolled Courses</h3><div class="card" *ngFor="let c of enrollment.enrolled$|async">{{c.name}}</div></section>`}) export class StudentProfileComponent {constructor(public enrollment:EnrollmentService){}}