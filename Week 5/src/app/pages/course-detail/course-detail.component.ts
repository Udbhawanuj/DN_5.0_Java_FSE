import { Component } from '@angular/core'; import { ActivatedRoute } from '@angular/router'; import { CourseService } from '../../services/course.service'; @Component({standalone:true,template:`<section class="container"><div class="card"><h2>{{course?.name}}</h2><p>Instructor: {{course?.instructor}}</p><p>Credits: {{course?.credits}}</p></div></section>`}) export class CourseDetailComponent {id=Number(this.route.snapshot.paramMap.get('id'));course=this.service.getLocal().find(c=>c.id===this.id);constructor(private route:ActivatedRoute,private service:CourseService){}}