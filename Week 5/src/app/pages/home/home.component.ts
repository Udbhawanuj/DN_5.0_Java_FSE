import { Component, OnDestroy, OnInit } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { CourseService } from '../../services/course.service'; @Component({standalone:true,imports:[FormsModule],template:`<section class="container"><h1>{{portalName}}</h1><p>Browse courses, enroll and track your academic progress.</p><div class="grid"><div class="card">Courses Available: {{count}}</div><div class="card">Enrolled: 3</div><div class="card">GPA: 3.8</div></div><input [(ngModel)]="searchTerm" placeholder="Search courses"><p>Searching for: {{searchTerm}}</p><button [disabled]="!isPortalActive" (click)="onEnrollClick()">Enroll Now</button><p>{{message}}</p></section>`}) export class HomeComponent implements OnInit,OnDestroy {portalName='Student Course Portal';isPortalActive=true;searchTerm='';message='';count=0;constructor(private courses:CourseService){}ngOnInit(){this.count=this.courses.getLocal().length;console.log('Home initialized')}ngOnDestroy(){console.log('Home destroyed')}onEnrollClick(){this.message='Enrollment opened!';}}