import { Component, OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { Router } from '@angular/router'; import { CourseCardComponent } from '../../components/course-card/course-card.component'; import { CourseService } from '../../services/course.service'; import { Course } from '../../models/course'; @Component({standalone:true,imports:[CommonModule,FormsModule,CourseCardComponent],template:`<section class="container"><h2>Courses</h2><input [(ngModel)]="search" (ngModelChange)="updateQuery()" placeholder="Filter courses"><p *ngIf="loading">Loading courses...</p><p class="error" *ngIf="error">{{error}}</p><div class="grid"><app-course-card *ngFor="let c of filtered();trackBy:trackById" [course]="c" (enroll)="select($event)"></app-course-card></div><p *ngIf="selectedId">Selected course ID: {{selectedId}}</p></section>`}) export class CourseListComponent implements OnInit {courses:Course[]=[];loading=true;error='';search='';selectedId?:number;constructor(private service:CourseService,private router:Router){}ngOnInit(){setTimeout(()=>{this.courses=this.service.getLocal();this.loading=false},1500)}filtered(){return this.courses.filter(c=>c.name.toLowerCase().includes(this.search.toLowerCase()))}trackById(_:number,c:Course){return c.id}select(id:number){this.selectedId=id;console.log('Selected',id)}updateQuery(){this.router.navigate([], {queryParams:{search:this.search},queryParamsHandling:'merge'})}}