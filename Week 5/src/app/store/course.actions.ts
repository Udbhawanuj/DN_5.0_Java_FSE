import { createAction, props } from '@ngrx/store'; import { Course } from '../models/course'; export const loadCourses=createAction('[Courses] Load'); export const loadCoursesSuccess=createAction('[Courses] Load Success',props<{courses:Course[]}>()); export const loadCoursesFailure=createAction('[Courses] Load Failure',props<{error:string}>()); export const toggleEnrollment=createAction('[Courses] Toggle Enrollment',props<{id:number}>());