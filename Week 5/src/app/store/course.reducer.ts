import { createReducer,on } from '@ngrx/store'; import { Course } from '../models/course'; import * as A from './course.actions'; export interface CourseState{courses:Course[];loading:boolean;error:string} export const initialState:CourseState={courses:[],loading:false,error:''}; export const courseReducer=createReducer(initialState,on(A.loadCourses,s=>({...s,loading:true})),on(A.loadCoursesSuccess,(s,{courses})=>({...s,courses,loading:false})),on(A.loadCoursesFailure,(s,{error})=>({...s,error,loading:false})),on(A.toggleEnrollment,(s,{id})=>({...s,courses:s.courses.map(c=>c.id===id?{...c,enrolled:!c.enrolled}:c)})));