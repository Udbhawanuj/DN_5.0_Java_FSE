import { TestBed } from '@angular/core/testing'; import { provideHttpClient } from '@angular/common/http'; import { provideHttpClientTesting,HttpTestingController } from '@angular/common/http/testing'; import { CourseService } from './course.service'; describe('CourseService',()=>{let s:CourseService;let h:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[CourseService,provideHttpClient(),provideHttpClientTesting()]});s=TestBed.inject(CourseService);h=TestBed.inject(HttpTestingController)});afterEach(()=>h.verify());it('requests courses URL',()=>{s.getCourses().subscribe(v=>expect(v.length).toBe(1));const r=h.expectOne('http://localhost:3000/courses');expect(r.request.method).toBe('GET');r.flush([{id:1,name:'A',instructor:'B',credits:3,category:'C',enrolled:false}])})});