import { Injectable } from '@angular/core'; import { BehaviorSubject } from 'rxjs'; import { Course } from '../models/course'; @Injectable({providedIn:'root'}) export class EnrollmentService { private enrolled=new BehaviorSubject<Course[]>([]); enrolled$=this.enrolled.asObservable(); toggle(c:Course){const a=this.enrolled.value; this.enrolled.next(a.some(x=>x.id===c.id)?a.filter(x=>x.id!==c.id):[...a,c]);} isEnrolled(id:number){return this.enrolled.value.some(x=>x.id===id);} }