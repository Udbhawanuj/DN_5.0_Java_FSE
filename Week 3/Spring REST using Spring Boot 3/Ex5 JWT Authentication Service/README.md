# Ex5 - JWT Authentication Service
In this exercise i created a simple authentication service that returns a JWT style token when valid credentials are passed


## Concepts Used
- REST POST endpoint
- Request body
- Authentication response
- JWT-style token generation
- HMAC SHA-256 signing
