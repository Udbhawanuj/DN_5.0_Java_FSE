server: { port: 8301 }
spring:
  application: { name: customer-service }
  datasource: { url: jdbc:h2:mem:customerservice;DB_CLOSE_DELAY=-1, username: sa, password: '' }
  jpa: { hibernate: { ddl-auto: update }, open-in-view: false }
eureka.client.service-url.defaultZone: http://localhost:8761/eureka
management.endpoints.web.exposure.include: health,info,metrics
---
spring.config.activate.on-profile: postgres
spring.datasource: { url: jdbc:postgresql://localhost:5432/week4, username: postgres, password: postgres }
