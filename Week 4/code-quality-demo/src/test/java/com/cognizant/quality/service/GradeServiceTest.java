package com.cognizant.quality.service;import org.junit.jupiter.api.*;import static org.junit.jupiter.api.Assertions.*;class GradeServiceTest{private final GradeService s=new GradeService();@Test void grades(){assertEquals("A",s.grade(95));assertEquals("B",s.grade(80));assertEquals("C",s.grade(65));assertEquals("D",s.grade(40));}@Test void invalid(){assertThrows(IllegalArgumentException.class,()->s.grade(-1));}}