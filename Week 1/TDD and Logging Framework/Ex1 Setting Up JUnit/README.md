# Setting Up JUnit
This exercise sets up a basic Java project with JUnit and I created a small `Calculator` class and wrote a simple unit test for the `add()` method


## How to Run
```bash
mvn test
```
