# Exercise 1 : Singleton Pattern
In this exercise I created a `Logger` class using the Singleton Design Pattern the main idea was to make sure that only one logger object is created and the same object is used throughout the program


## Concepts Used
* Singleton Pattern
* Private constructor
* Static object
* `getInstance()` method
* Checking if both objects are the same
